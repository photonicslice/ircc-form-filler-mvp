/**
 * Environment configuration
 * MUST be imported before any other modules that use env variables
 */
import dotenv from 'dotenv';

// Load .env file
dotenv.config();

// Export nothing - this file just needs to execute
export default {};
